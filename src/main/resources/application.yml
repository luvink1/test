spring:
  application:
    name: springboot04-project
  datasource:
    url: jdbc:mysql://localhost:3306/luvink?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: Zmvfgrty1
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:mapper/*.xml